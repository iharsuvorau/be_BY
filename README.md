# Belarusian Keyboard Layout for Mac OS X

To install the layout you need to:

- copy the bundle file to `~/Library/Keyboard Layouts/`
- open `System Preferences` -- `Keyboard` -- `Input Sources`
- press `+` and choose `Belarusian`
